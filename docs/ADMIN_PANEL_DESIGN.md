# Arrakis Labs - Admin Panel System Design

> **Design Principle**: This admin panel extends the existing Dune-inspired UI system. All components reuse the established color palette (`#0A0A08`, `#1A1814`, `#E8E4D9`, `#78716C`, `#F59E0B`, `#D97706`, `#92400E`), typography (Rajdhani, Orbitron, Inter), and interaction patterns.

---

## A. Information Architecture

### Sidebar Navigation Structure

```
┌─────────────────────────────────────────────────────────────┐
│  ARRAKIS ADMIN                                              │
│  ─────────────────                                          │
│                                                             │
│  📊 DASHBOARD                          [View-Only]          │
│                                                             │
│  ═══ CONTENT ═══                                            │
│  📝 Problems                           [Mutating]           │
│  🏆 Contests                           [Mutating]           │
│  📋 Test Cases                         [Mutating]           │
│                                                             │
│  ═══ EXECUTION ═══                                          │
│  ⚙️ Judge Configuration                [Mutating]           │
│  📊 Submissions                        [View-Only]          │
│  🔍 Plagiarism Review                  [Mutating]           │
│                                                             │
│  ═══ COMPETITION ═══                                        │
│  🏅 Leaderboards                       [Mutating]           │
│                                                             │
│  ═══ USERS ═══                                              │
│  👥 User Management                    [Mutating]           │
│  🛡️ Roles & Permissions               [Mutating]           │
│                                                             │
│  ═══ SYSTEM ═══                                             │
│  🖥️ Services Health                   [View-Only]          │
│  📜 Audit Logs                        [View-Only]          │
│  ⚡ Queue Monitor                      [View-Only]          │
│                                                             │
│  ─────────────────                                          │
│  ⚠️ DANGER ZONE                        [Destructive]        │
└─────────────────────────────────────────────────────────────┘
```

### Action Classification

| Category        | Actions                         | UI Treatment                     |
| --------------- | ------------------------------- | -------------------------------- |
| **View-Only**   | List, Search, Filter, Export    | Default styling, no confirmation |
| **Mutating**    | Create, Edit, Update, Configure | Amber border on active state     |
| **Destructive** | Delete, Ban, Purge, Reset       | Red accent, double confirmation  |

---

## B. Detailed Admin Modules

---

### Module 1: Problem Management

#### Purpose

Complete lifecycle management of coding problems including creation, difficulty assignment, constraints, and test case management.

#### Screens/Pages

| Page              | Route                            | Purpose                        |
| ----------------- | -------------------------------- | ------------------------------ |
| Problem List      | `/admin/problems`                | Paginated table with filters   |
| Problem Editor    | `/admin/problems/new`            | Create new problem             |
| Problem Editor    | `/admin/problems/:id/edit`       | Edit existing problem          |
| Test Case Manager | `/admin/problems/:id/testcases`  | Manage test cases              |
| Difficulty Config | `/admin/problems/:id/difficulty` | AI-assisted difficulty setting |

#### Key Fields & Tables

**Problems Table View**
| Column | Type | Sortable | Filterable |
|--------|------|----------|------------|
| ID | String | ✓ | ✓ |
| Title | String | ✓ | ✓ (search) |
| Difficulty | Enum | ✓ | ✓ |
| Category | String[] | ✓ | ✓ |
| Status | Enum | ✓ | ✓ |
| Submissions | Number | ✓ | - |
| Acceptance Rate | Percent | ✓ | - |
| Created | Date | ✓ | ✓ |
| Author | Ref | - | ✓ |

**Problem Editor Fields**

```
┌─────────────────────────────────────────────────────────────┐
│ PROBLEM DETAILS                                             │
├─────────────────────────────────────────────────────────────┤
│ Title*           [________________________________]         │
│ Slug*            [________________________________] [Auto]  │
│ Category*        [▼ Select categories____________]         │
│ Tags             [___________] [+ Add Tag]                  │
│ Status           ◉ Draft  ○ Review  ○ Published  ○ Hidden  │
├─────────────────────────────────────────────────────────────┤
│ PROBLEM STATEMENT (Markdown)                                │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ # Description                                           │ │
│ │ Given an array of integers...                           │ │
│ └─────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────┤
│ CONSTRAINTS                                                 │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 1 ≤ n ≤ 10^5                                            │ │
│ │ -10^9 ≤ nums[i] ≤ 10^9                                  │ │
│ └─────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────┤
│ HINTS (Optional)                                            │
│ [+ Add Hint]                                                │
│ 1. Consider using a hash map... [↑] [↓] [×]                │
│ 2. Think about the complement... [↑] [↓] [×]               │
└─────────────────────────────────────────────────────────────┘
```

#### Difficulty Assignment Logic

**Difficulty Drawer Panel**

```
┌─────────────────────────────────────────────────────────────┐
│ DIFFICULTY CONFIGURATION                              [×]   │
├─────────────────────────────────────────────────────────────┤
│ Manual Assignment                                           │
│ ○ Easy   ○ Medium   ○ Hard                                 │
├─────────────────────────────────────────────────────────────┤
│ AI RECOMMENDATION                    [🔄 Re-analyze]       │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ Suggested: MEDIUM                                       │ │
│ │ Confidence: 87%                                         │ │
│ │                                                         │ │
│ │ Factors:                                                │ │
│ │ • Algorithm complexity: O(n) → Easy                     │ │
│ │ • Concept prerequisites: 2 → Medium                     │ │
│ │ • Similar problems avg: Medium                          │ │
│ │ • Edge case complexity: High → Hard                     │ │
│ └─────────────────────────────────────────────────────────┘ │
│ [Apply AI Suggestion]                                       │
├─────────────────────────────────────────────────────────────┤
│ Historical Data                                             │
│ • First-attempt success rate: 34%                          │
│ • Average solve time: 18 min                               │
│ • Community rating: 3.2/5 (142 votes)                      │
└─────────────────────────────────────────────────────────────┘
```

#### Time & Memory Limits (Language-Aware)

**Constraints Configuration Panel**

```
┌─────────────────────────────────────────────────────────────┐
│ EXECUTION LIMITS                                            │
├─────────────────────────────────────────────────────────────┤
│ Base Limits (Applied to all languages)                      │
│ Time Limit:    [___2___] seconds                           │
│ Memory Limit:  [__256___] MB                               │
├─────────────────────────────────────────────────────────────┤
│ Language Multipliers                                        │
│ ┌───────────────┬──────────┬──────────┬─────────┐          │
│ │ Language      │ Time (×) │ Memory(×)│ Enabled │          │
│ ├───────────────┼──────────┼──────────┼─────────┤          │
│ │ Python        │   2.0    │   1.5    │   ✓     │          │
│ │ JavaScript    │   1.5    │   1.2    │   ✓     │          │
│ │ Java          │   1.5    │   2.0    │   ✓     │          │
│ │ C++           │   1.0    │   1.0    │   ✓     │          │
│ │ Go            │   1.0    │   1.0    │   ✓     │          │
│ │ Rust          │   1.0    │   1.0    │   ☐     │          │
│ └───────────────┴──────────┴──────────┴─────────┘          │
│                                                             │
│ Effective Limits Preview:                                   │
│ Python: 4s / 384MB | C++: 2s / 256MB | Java: 3s / 512MB    │
└─────────────────────────────────────────────────────────────┘
```

#### Test Case Management

**Test Cases Page**

```
┌─────────────────────────────────────────────────────────────┐
│ TEST CASES - Two Sum                    [+ Add Test Case]  │
├─────────────────────────────────────────────────────────────┤
│ SAMPLE TEST CASES (Visible to users)                        │
│ ┌─────┬──────────────────┬──────────────────┬─────┬──────┐ │
│ │ #   │ Input            │ Expected Output  │Score│ Act  │ │
│ ├─────┼──────────────────┼──────────────────┼─────┼──────┤ │
│ │ S1  │ [2,7,11,15], 9   │ [0,1]           │  0  │ ✎ ×  │ │
│ │ S2  │ [3,2,4], 6       │ [1,2]           │  0  │ ✎ ×  │ │
│ │ S3  │ [3,3], 6         │ [0,1]           │  0  │ ✎ ×  │ │
│ └─────┴──────────────────┴──────────────────┴─────┴──────┘ │
│                                        [+ Add Sample Case]  │
├─────────────────────────────────────────────────────────────┤
│ HIDDEN TEST CASES (Not visible to users)                    │
│ ┌─────┬──────────────────┬──────────────────┬─────┬──────┐ │
│ │ #   │ Input            │ Expected Output  │Score│ Act  │ │
│ ├─────┼──────────────────┼──────────────────┼─────┼──────┤ │
│ │ H1  │ [Large array...] │ [Expected...]    │ 10  │ ✎ ×  │ │
│ │ H2  │ Edge: empty arr  │ []               │ 10  │ ✎ ×  │ │
│ │ H3  │ Edge: negatives  │ [...]            │ 10  │ ✎ ×  │ │
│ │ ... │                  │                  │     │      │ │
│ │ H15 │ Stress: 10^5     │ [...]            │ 20  │ ✎ ×  │ │
│ └─────┴──────────────────┴──────────────────┴─────┴──────┘ │
│                                        [+ Add Hidden Case]  │
│                                        [📤 Import from File]│
├─────────────────────────────────────────────────────────────┤
│ TEST CASE GENERATOR                                         │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ Generate random test cases:                             │ │
│ │ Count: [__10__]  Min N: [__1__]  Max N: [__1000__]     │ │
│ │ [Generate] [Preview] [Add to Hidden]                    │ │
│ └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

#### Admin Actions

| Action            | Trigger              | Confirmation             | Audit |
| ----------------- | -------------------- | ------------------------ | ----- |
| Create Problem    | Button               | None                     | Yes   |
| Edit Problem      | Row click/button     | None                     | Yes   |
| Change Status     | Dropdown             | Modal if Published→Draft | Yes   |
| Delete Problem    | Button (danger zone) | Type slug to confirm     | Yes   |
| Bulk Delete       | Checkbox + action    | Type "DELETE" to confirm | Yes   |
| Duplicate Problem | Context menu         | None                     | Yes   |
| Import Problems   | Button               | Preview modal            | Yes   |
| Export Problems   | Button               | None                     | Yes   |

#### Validation Rules

```javascript
// Problem Validation Schema
{
  title: {
    required: true,
    minLength: 5,
    maxLength: 200,
    unique: true
  },
  slug: {
    required: true,
    pattern: /^[a-z0-9-]+$/,
    unique: true
  },
  statement: {
    required: true,
    minLength: 100,
    format: 'markdown'
  },
  category: {
    required: true,
    minItems: 1,
    maxItems: 5,
    enum: ['Arrays', 'Strings', 'Trees', 'Graphs', ...]
  },
  difficulty: {
    required: true,
    enum: ['Easy', 'Medium', 'Hard']
  },
  constraints: {
    required: true,
    minLength: 10
  },
  timeLimitMs: {
    required: true,
    min: 100,
    max: 30000,
    default: 2000
  },
  memoryLimitMB: {
    required: true,
    min: 16,
    max: 1024,
    default: 256
  },
  sampleTestCases: {
    required: true,
    minItems: 1,
    maxItems: 5
  },
  hiddenTestCases: {
    required: true,
    minItems: 3
  }
}
```

#### Edge Cases

| Edge Case                     | Handling                             |
| ----------------------------- | ------------------------------------ |
| Duplicate slug                | Auto-append numeric suffix           |
| Missing test cases on publish | Block publish, show error            |
| Referenced in active contest  | Prevent deletion, show warning       |
| Very long input/output        | Truncate display, full view in modal |
| Invalid markdown              | Show preview with warnings           |
| Circular problem references   | Validate and reject                  |

---

### Module 2: Contest Management

#### Purpose

Full lifecycle management of coding contests from draft creation through archival.

#### Contest Lifecycle State Machine

```
┌─────────┐    publish    ┌───────────┐   start time   ┌──────┐
│  DRAFT  │ ────────────► │ SCHEDULED │ ─────────────► │ LIVE │
└─────────┘               └───────────┘                └──────┘
     │                          │                          │
     │ delete                   │ cancel                   │ end time
     ▼                          ▼                          ▼
┌─────────┐               ┌───────────┐               ┌──────────┐
│ DELETED │               │ CANCELLED │               │ ARCHIVED │
└─────────┘               └───────────┘               └──────────┘
```

#### Screens/Pages

| Page            | Route                              | Purpose                          |
| --------------- | ---------------------------------- | -------------------------------- |
| Contest List    | `/admin/contests`                  | All contests with status filters |
| Contest Editor  | `/admin/contests/new`              | Create new contest               |
| Contest Editor  | `/admin/contests/:id/edit`         | Edit draft/scheduled contest     |
| Problem Mapping | `/admin/contests/:id/problems`     | Assign problems to contest       |
| Participants    | `/admin/contests/:id/participants` | View/manage participants         |
| Live Monitor    | `/admin/contests/:id/live`         | Real-time contest monitoring     |

#### Contest Editor

```
┌─────────────────────────────────────────────────────────────┐
│ CONTEST CONFIGURATION                                       │
├─────────────────────────────────────────────────────────────┤
│ Basic Information                                           │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ Title*        [________________________________]        │ │
│ │ Description   [________________________________]        │ │
│ │               [________________________________]        │ │
│ │ Banner Image  [📤 Upload] [Preview]                     │ │
│ └─────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────┤
│ Schedule                                                    │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ Start Date*   [📅 2026-01-20] Time* [⏰ 14:00]  UTC    │ │
│ │ End Date*     [📅 2026-01-20] Time* [⏰ 17:00]  UTC    │ │
│ │ Duration:     3 hours                                   │ │
│ │                                                         │ │
│ │ Registration                                            │ │
│ │ ☑ Require Registration                                 │ │
│ │ Opens: [📅 2026-01-15] Closes: [📅 2026-01-19]        │ │
│ └─────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────┤
│ Visibility & Access                                         │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ Visibility:  ◉ Public  ○ Private  ○ Invite-Only       │ │
│ │                                                         │ │
│ │ If Private/Invite-Only:                                │ │
│ │ Access Code: [AUTO_GENERATED] [🔄 Regenerate]          │ │
│ │ Invite List: [📤 Import CSV] [✉️ Send Invites]         │ │
│ └─────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────┤
│ Scoring Rules                                               │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ Scoring Type: ◉ ICPC  ○ IOI  ○ Custom                 │ │
│ │                                                         │ │
│ │ ICPC Settings:                                          │ │
│ │ Penalty per wrong submission: [__20__] minutes         │ │
│ │                                                         │ │
│ │ Tiebreaker: ◉ Total Time  ○ Last Accepted Submission  │ │
│ └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

#### Problem Mapping Interface

```
┌─────────────────────────────────────────────────────────────┐
│ CONTEST PROBLEMS - Weekly Challenge #42         [+ Add]    │
├─────────────────────────────────────────────────────────────┤
│ ┌─────┬────────────────────────┬────────┬───────┬────────┐ │
│ │Order│ Problem                │Difficulty│Points│ Actions│ │
│ ├─────┼────────────────────────┼────────┼───────┼────────┤ │
│ │ A   │ Two Sum                │ Easy   │  100  │ ⬆⬇ ✎ × │ │
│ │ B   │ Valid Parentheses      │ Easy   │  100  │ ⬆⬇ ✎ × │ │
│ │ C   │ LRU Cache              │ Medium │  200  │ ⬆⬇ ✎ × │ │
│ │ D   │ Word Ladder            │ Medium │  300  │ ⬆⬇ ✎ × │ │
│ │ E   │ Median of Two Arrays   │ Hard   │  500  │ ⬆⬇ ✎ × │ │
│ └─────┴────────────────────────┴────────┴───────┴────────┘ │
│                                                             │
│ [+ Add Problem]  [🔀 Auto-arrange by difficulty]           │
├─────────────────────────────────────────────────────────────┤
│ Point Distribution Preview:                                 │
│ Total: 1200 points | Easy: 200 | Medium: 500 | Hard: 500   │
└─────────────────────────────────────────────────────────────┘
```

#### Admin Actions

| Action      | Allowed States   | Confirmation   | Notes                   |
| ----------- | ---------------- | -------------- | ----------------------- |
| Save Draft  | DRAFT            | None           | Auto-save enabled       |
| Publish     | DRAFT            | Modal          | Validates all fields    |
| Edit        | DRAFT, SCHEDULED | None           | Creates version history |
| Cancel      | SCHEDULED        | Type title     | Notifies registrants    |
| Force End   | LIVE             | Double confirm | Emergency only          |
| Extend Time | LIVE             | Modal          | Notifies participants   |
| Archive     | LIVE (ended)     | None           | Auto after 7 days       |
| Clone       | Any              | None           | Creates new DRAFT       |
| Delete      | DRAFT, CANCELLED | Type title     | Permanent               |

#### Visibility Rules Matrix

| Contest State | Problems Visible | Solutions Visible | Leaderboard |
| ------------- | ---------------- | ----------------- | ----------- |
| DRAFT         | Admin only       | Admin only        | Hidden      |
| SCHEDULED     | Titles only      | Hidden            | Hidden      |
| LIVE          | Full             | Hidden            | Real-time   |
| LIVE (frozen) | Full             | Hidden            | Frozen      |
| ARCHIVED      | Full             | Full              | Final       |

---

### Module 3: Submission & Execution Control

#### Purpose

Configure judge behavior, language settings, and monitor submission execution.

#### Screens/Pages

| Page              | Route                        | Purpose                  |
| ----------------- | ---------------------------- | ------------------------ |
| Judge Config      | `/admin/execution/config`    | Global judge settings    |
| Languages         | `/admin/execution/languages` | Language configurations  |
| Submissions       | `/admin/submissions`         | View all submissions     |
| Submission Detail | `/admin/submissions/:id`     | Single submission detail |

#### Judge Configuration Panel

```
┌─────────────────────────────────────────────────────────────┐
│ JUDGE CONFIGURATION                                         │
├─────────────────────────────────────────────────────────────┤
│ Global Settings                                             │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ Max Concurrent Executions:  [__50__]                    │ │
│ │ Queue Timeout (seconds):    [__300__]                   │ │
│ │ Max Output Size (KB):       [__64__]                    │ │
│ │ Max Source Size (KB):       [__128__]                   │ │
│ └─────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────┤
│ Execution Behavior                                          │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ On Compilation Error:   ◉ Stop  ○ Continue             │ │
│ │ On Runtime Error:       ◉ Stop  ○ Continue             │ │
│ │ On TLE (first):         ○ Stop  ◉ Continue             │ │
│ │                                                         │ │
│ │ Comparison Mode:        ◉ Exact  ○ Ignore Whitespace   │ │
│ │                         ○ Ignore Trailing  ○ Custom    │ │
│ │                                                         │ │
│ │ Float Tolerance:        [__1e-6__] (for numeric output)│ │
│ └─────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────┤
│ Rate Limiting                                               │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ Submissions per user per problem per minute: [__5__]   │ │
│ │ Submissions per user per hour:                [__100__]│ │
│ │ Global submissions per minute:                [__500__]│ │
│ └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

#### Language Configuration

```
┌─────────────────────────────────────────────────────────────┐
│ LANGUAGE SETTINGS                        [+ Add Language]  │
├─────────────────────────────────────────────────────────────┤
│ ┌────────────┬─────────┬──────────┬───────┬───────┬──────┐ │
│ │ Language   │ Version │ Compiler │ Time× │ Mem×  │Enable│ │
│ ├────────────┼─────────┼──────────┼───────┼───────┼──────┤ │
│ │ Python     │ 3.11.4  │ CPython  │  2.0  │  1.5  │  ✓   │ │
│ │ JavaScript │ 18.17   │ Node.js  │  1.5  │  1.2  │  ✓   │ │
│ │ Java       │ 17.0.8  │ OpenJDK  │  1.5  │  2.0  │  ✓   │ │
│ │ C++        │ 17      │ g++ 12.2 │  1.0  │  1.0  │  ✓   │ │
│ │ Go         │ 1.21    │ go       │  1.0  │  1.0  │  ✓   │ │
│ │ Rust       │ 1.72    │ rustc    │  1.0  │  1.0  │  ☐   │ │
│ │ C#         │ 11      │ .NET 7   │  1.5  │  2.0  │  ☐   │ │
│ └────────────┴─────────┴──────────┴───────┴───────┴──────┘ │
│                                                             │
│ [Edit Selected] [Bulk Enable/Disable]                       │
└─────────────────────────────────────────────────────────────┘
```

#### Submissions Table

```
┌─────────────────────────────────────────────────────────────┐
│ SUBMISSIONS                      [Export] [🔄 Refresh]     │
├─────────────────────────────────────────────────────────────┤
│ Filters:                                                    │
│ [User: ____] [Problem: ▼] [Status: ▼] [Language: ▼]        │
│ [Date Range: ▼] [Contest: ▼]                               │
├─────────────────────────────────────────────────────────────┤
│ ┌──────┬────────┬────────────┬────────┬───────┬───────────┐│
│ │ ID   │ User   │ Problem    │ Status │ Lang  │ Time      ││
│ ├──────┼────────┼────────────┼────────┼───────┼───────────┤│
│ │#4521 │john_d  │Two Sum     │✓ AC    │Python │ 2m ago    ││
│ │#4520 │alice_w │LRU Cache   │✗ WA    │C++    │ 5m ago    ││
│ │#4519 │bob_m   │Word Ladder │⏱ TLE   │Java   │ 8m ago    ││
│ │#4518 │john_d  │Two Sum     │✗ RE    │Python │ 12m ago   ││
│ │ ...  │        │            │        │       │           ││
│ └──────┴────────┴────────────┴────────┴───────┴───────────┘│
│                                                             │
│ [← Page 1 of 234 →]                                        │
└─────────────────────────────────────────────────────────────┘
```

---

### Module 4: Leaderboard Management

#### Purpose

Control ranking display, freeze/unfreeze during contests, and handle manual recalculations.

#### Screens/Pages

| Page                 | Route                            | Purpose                   |
| -------------------- | -------------------------------- | ------------------------- |
| Global Leaderboard   | `/admin/leaderboards/global`     | Overall platform rankings |
| Contest Leaderboards | `/admin/leaderboards/contests`   | Per-contest rankings      |
| Leaderboard Config   | `/admin/leaderboards/:id/config` | Ranking rules             |

#### Leaderboard Control Panel

```
┌─────────────────────────────────────────────────────────────┐
│ LEADERBOARD - Weekly Challenge #42                          │
├─────────────────────────────────────────────────────────────┤
│ Status: 🟢 LIVE          Participants: 1,247               │
│ Contest Ends: 2h 15m     Last Updated: 30s ago             │
├─────────────────────────────────────────────────────────────┤
│ Controls:                                                   │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ [🔒 FREEZE LEADERBOARD]  [🔄 Force Recalculate]        │ │
│ │                                                         │ │
│ │ Auto-freeze: ☑ Enable  Before end: [__60__] minutes    │ │
│ │                                                         │ │
│ │ Visibility:  ◉ Public  ○ Participants Only  ○ Hidden   │ │
│ └─────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────┤
│ Ranking Table                                               │
│ ┌────┬────────────┬────┬─────┬───────┬───────┬───────────┐ │
│ │Rank│ User       │ A  │  B  │   C   │ Score │ Penalty   │ │
│ ├────┼────────────┼────┼─────┼───────┼───────┼───────────┤ │
│ │ 1  │ alice_w    │ ✓  │ ✓   │ ✓(3)  │  600  │ 1:23:45   │ │
│ │ 2  │ john_d     │ ✓  │ ✓   │ ✓(1)  │  600  │ 1:45:12   │ │
│ │ 3  │ bob_m      │ ✓  │ ✓   │ -     │  400  │ 0:58:33   │ │
│ │ 4  │ carol_s    │ ✓  │ ✓(2)│ -     │  400  │ 1:12:44   │ │
│ │...│            │    │     │       │       │           │ │
│ └────┴────────────┴────┴─────┴───────┴───────┴───────────┘ │
│                                                             │
│ [Export CSV] [Export JSON] [View Full]                      │
└─────────────────────────────────────────────────────────────┘
```

#### Ranking Logic Configuration

```javascript
// Ranking Strategies
{
  ICPC: {
    primarySort: 'problemsSolved',
    secondarySort: 'totalPenalty',
    penaltyPerWrongSubmission: 20, // minutes
    penaltyCalculation: 'timeFromStart + wrongAttempts * penalty'
  },
  IOI: {
    primarySort: 'totalScore',
    secondarySort: 'lastSubmissionTime',
    partialScoring: true,
    subtaskScoring: true
  },
  Custom: {
    primarySort: 'configurable',
    secondarySort: 'configurable',
    formula: 'customizable'
  }
}
```

#### Admin Actions

| Action            | Effect                      | Confirmation        |
| ----------------- | --------------------------- | ------------------- |
| Freeze            | Hides updates from public   | Modal with duration |
| Unfreeze          | Reveals all pending updates | Modal               |
| Force Recalculate | Re-scores all submissions   | Yes, shows progress |
| Export            | Downloads CSV/JSON          | None                |
| Hide User         | Removes from public view    | Modal               |
| Adjust Score      | Manual score override       | Requires reason     |

---

### Module 5: Plagiarism Detection

#### Purpose

Detect, review, and take action on suspected code plagiarism.

#### Detection Triggers

```
┌─────────────────────────────────────────────────────────────┐
│ PLAGIARISM DETECTION SETTINGS                               │
├─────────────────────────────────────────────────────────────┤
│ Automatic Detection Triggers                                │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ ☑ After every contest ends                             │ │
│ │ ☑ When similarity > 85% detected                       │ │
│ │ ☑ When same rare bug pattern found                     │ │
│ │ ☑ When submission timestamps suspicious                │ │
│ │ ☐ Real-time during contest (resource intensive)        │ │
│ └─────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────┤
│ Similarity Thresholds                                       │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ Flag for Review:    [__70__]%                          │ │
│ │ Auto-flag:          [__85__]%                          │ │
│ │ Likely Plagiarism:  [__95__]%                          │ │
│ │                                                         │ │
│ │ Detection Methods:                                      │ │
│ │ ☑ Token-based (AST)                                    │ │
│ │ ☑ String similarity (Levenshtein)                      │ │
│ │ ☑ Structural similarity (MOSS-like)                    │ │
│ │ ☑ Variable renaming detection                          │ │
│ └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

#### Review Interface

```
┌─────────────────────────────────────────────────────────────┐
│ PLAGIARISM REVIEW QUEUE                  [Run Detection]   │
├─────────────────────────────────────────────────────────────┤
│ Filter: [All ▼] [Contest ▼] [Problem ▼] [Status ▼]         │
├─────────────────────────────────────────────────────────────┤
│ ┌───────┬──────────────┬───────────┬────────┬─────────────┐│
│ │Status │ Users        │ Problem   │Similar │ Contest     ││
│ ├───────┼──────────────┼───────────┼────────┼─────────────┤│
│ │🟡 New │john_d↔alice_w│Two Sum    │  92%   │Weekly #42   ││
│ │🟡 New │bob_m ↔ eve_t │LRU Cache  │  87%   │Weekly #42   ││
│ │🔵 Rev │carol_s↔dan_k │Word Ladder│  78%   │Monthly #5   ││
│ │✓ Clear│tom_h ↔ sam_r │Merge Sort │  71%   │Practice     ││
│ │⚠ Flag │user_a↔user_b │Graph DFS  │  95%   │Weekly #41   ││
│ └───────┴──────────────┴───────────┴────────┴─────────────┘│
└─────────────────────────────────────────────────────────────┘
```

#### Side-by-Side Comparison (Drawer)

```
┌─────────────────────────────────────────────────────────────┐
│ COMPARISON: john_d vs alice_w - Two Sum        [×] Close   │
├──────────────────────────────┬──────────────────────────────┤
│ john_d - Submitted 14:23:45  │ alice_w - Submitted 14:25:12 │
├──────────────────────────────┼──────────────────────────────┤
│ def two_sum(nums, target):   │ def two_sum(nums, target):   │
│   seen = {}                  │   hashmap = {}               │
│   for i, num in enumerate(.. │   for i, num in enumerate(.. │
│ ┃   if target - num in seen: │ ┃   if target - num in hashm │
│ ┃     return [seen[target -..│ ┃     return [hashmap[target │
│ ┃   seen[num] = i            │ ┃   hashmap[num] = i         │
│   return []                  │   return []                  │
├──────────────────────────────┴──────────────────────────────┤
│ Similarity: 92% | Matched Lines: 6/7 | Method: AST + Token  │
├─────────────────────────────────────────────────────────────┤
│ Admin Actions:                                              │
│ [✓ Clear - Not Plagiarism]  [⚠ Flag Both Users]            │
│ [Flag john_d only]          [Flag alice_w only]            │
│                                                             │
│ Notes: [_____________________________________________]      │
│        [_____________________________________________]      │
│                                                             │
│ [Submit Decision]                                           │
└─────────────────────────────────────────────────────────────┘
```

#### Override Workflow

```
Admin Decision Flow:
1. Review case → Add notes
2. Make decision:
   - CLEAR: Mark as reviewed, no action
   - FLAG: Mark submissions as plagiarized
   - ESCALATE: Send to senior admin
3. Apply consequences (if flagged):
   - Zero score for submissions
   - Warning to user(s)
   - Temporary ban
   - Permanent ban
4. Notify affected users
5. Log decision in audit trail
```

---

### Module 6: User & Role Management

#### Purpose

Manage user accounts, permissions, and disciplinary actions.

#### Screens/Pages

| Page        | Route                | Purpose                       |
| ----------- | -------------------- | ----------------------------- |
| User List   | `/admin/users`       | Search and manage users       |
| User Detail | `/admin/users/:id`   | Full user profile and history |
| Roles       | `/admin/roles`       | Role definitions              |
| Permissions | `/admin/permissions` | Permission matrix             |

#### User Management Interface

```
┌─────────────────────────────────────────────────────────────┐
│ USER MANAGEMENT                           [+ Create User]  │
├─────────────────────────────────────────────────────────────┤
│ Search: [________________________] [🔍]                    │
│ Filter: [Role ▼] [Status ▼] [Registration Date ▼]          │
├─────────────────────────────────────────────────────────────┤
│ ┌──────┬─────────────┬─────────┬────────┬───────┬────────┐ │
│ │ ID   │ User        │ Email   │ Role   │Status │ Action │ │
│ ├──────┼─────────────┼─────────┼────────┼───────┼────────┤ │
│ │ 1245 │ john_doe    │j@e.com  │ User   │Active │ ⋮      │ │
│ │ 1244 │ alice_admin │a@e.com  │ Admin  │Active │ ⋮      │ │
│ │ 1243 │ bob_banned  │b@e.com  │ User   │Banned │ ⋮      │ │
│ │ 1242 │ carol_user  │c@e.com  │ User   │Suspend│ ⋮      │ │
│ └──────┴─────────────┴─────────┴────────┴───────┴────────┘ │
│                                                             │
│ Bulk Actions: [Select All] [Ban Selected] [Export]         │
└─────────────────────────────────────────────────────────────┘
```

#### User Detail View

```
┌─────────────────────────────────────────────────────────────┐
│ USER: john_doe                                              │
├─────────────────────────────────────────────────────────────┤
│ Profile                        │ Statistics                 │
│ ─────────────                  │ ──────────                 │
│ ID: 1245                       │ Problems Solved: 145       │
│ Email: john@example.com        │ Contests: 23               │
│ Role: User                     │ Best Rank: #12             │
│ Status: Active                 │ Rating: 1856               │
│ Registered: 2025-06-15         │ Submissions: 892           │
│ Last Active: 2 hours ago       │                            │
├─────────────────────────────────────────────────────────────┤
│ Admin Actions                                               │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ [Change Role ▼]  [⚠ Warn]  [⏸ Suspend]  [🚫 Ban]       │ │
│ │                                                         │ │
│ │ Quick Actions:                                          │ │
│ │ • Reset Password  • Verify Email  • Clear Sessions     │ │
│ │ • View Submissions  • View IP History  • Export Data   │ │
│ └─────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────┤
│ Moderation History                                          │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 2025-12-01: Warning issued for minor violation (admin1)│ │
│ │ 2025-11-15: Plagiarism case cleared (admin2)           │ │
│ └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

#### Disciplinary Actions

| Action   | Duration Options           | Reversible       | Notification   |
| -------- | -------------------------- | ---------------- | -------------- |
| Warn     | N/A                        | N/A              | Email + In-app |
| Suspend  | 1d, 7d, 30d, Custom        | Yes              | Email          |
| Ban      | Permanent                  | Review after 6mo | Email          |
| Flag     | Until reviewed             | Yes              | None           |
| Restrict | Custom (e.g., no contests) | Yes              | Email          |

---

### Module 7: System / Service Management

#### Purpose

Monitor platform health, judge workers, AI services, and system queues.

#### Screens/Pages

| Page          | Route                  | Purpose                   |
| ------------- | ---------------------- | ------------------------- |
| Dashboard     | `/admin/system`        | Overview of all services  |
| Judge Workers | `/admin/system/judges` | Judge instance management |
| AI Services   | `/admin/system/ai`     | AI service health         |
| Queue Monitor | `/admin/system/queues` | Job queue status          |
| Audit Logs    | `/admin/system/audit`  | Admin action history      |

#### System Dashboard

```
┌─────────────────────────────────────────────────────────────┐
│ SYSTEM HEALTH                            Last check: 30s   │
├─────────────────────────────────────────────────────────────┤
│ ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │
│ │ 🟢 BACKEND  │  │ 🟢 DATABASE │  │ 🟡 JUDGES   │          │
│ │ API: 99.9%  │  │ MongoDB OK  │  │ 4/5 Active  │          │
│ │ Latency: 45ms │ │ Conn: 42/100│  │ Queue: 127  │          │
│ └─────────────┘  └─────────────┘  └─────────────┘          │
│                                                             │
│ ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │
│ │ 🟢 AI SVC   │  │ 🟢 CACHE    │  │ 🟢 STORAGE  │          │
│ │ FastAPI OK  │  │ Redis OK    │  │ S3 OK       │          │
│ │ Load: 23%   │  │ Hit: 94%    │  │ Used: 67%   │          │
│ └─────────────┘  └─────────────┘  └─────────────┘          │
├─────────────────────────────────────────────────────────────┤
│ Recent Alerts                                               │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ 🟡 15:30 - Judge worker-03 unresponsive, restarting    │ │
│ │ 🟢 15:25 - Judge worker-03 recovered                   │ │
│ │ 🟡 14:45 - High queue depth detected (>100)            │ │
│ └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

#### Judge Worker Management

```
┌─────────────────────────────────────────────────────────────┐
│ JUDGE WORKERS                     [+ Scale Up] [- Scale Dn]│
├─────────────────────────────────────────────────────────────┤
│ ┌──────────┬────────┬─────────┬───────────┬───────┬──────┐ │
│ │ Worker   │ Status │ Current │ Processed │ Errors│ Act  │ │
│ ├──────────┼────────┼─────────┼───────────┼───────┼──────┤ │
│ │ judge-01 │ 🟢 OK  │ Java    │   1,234   │   3   │ ⋮    │ │
│ │ judge-02 │ 🟢 OK  │ Python  │   1,156   │   1   │ ⋮    │ │
│ │ judge-03 │ 🟡 Slow│ C++     │     987   │  12   │ ⋮    │ │
│ │ judge-04 │ 🟢 OK  │ Idle    │   1,089   │   2   │ ⋮    │ │
│ │ judge-05 │ 🔴 Down│ -       │     756   │  45   │ ⋮    │ │
│ └──────────┴────────┴─────────┴───────────┴───────┴──────┘ │
│                                                             │
│ Actions: [Restart All] [Clear Queue] [View Logs]           │
└─────────────────────────────────────────────────────────────┘
```

#### Audit Log

```
┌─────────────────────────────────────────────────────────────┐
│ AUDIT LOG                        [Export] [Filter ▼]       │
├─────────────────────────────────────────────────────────────┤
│ ┌──────────────┬─────────────┬──────────────────────┬─────┐│
│ │ Timestamp    │ Admin       │ Action               │ Obj ││
│ ├──────────────┼─────────────┼──────────────────────┼─────┤│
│ │ 15:45:23     │ alice_admin │ problem.update       │ #123││
│ │ 15:44:01     │ bob_admin   │ user.ban             │ #456││
│ │ 15:42:55     │ alice_admin │ contest.publish      │ #42 ││
│ │ 15:40:12     │ system      │ judge.restart        │ #03 ││
│ │ 15:38:44     │ alice_admin │ leaderboard.freeze   │ #42 ││
│ └──────────────┴─────────────┴──────────────────────┴─────┘│
│                                                             │
│ [View Details] - Shows full before/after state             │
└─────────────────────────────────────────────────────────────┘
```

---

## C. Technical Deliverables

### 1. React Admin Route Structure

```jsx
// src/routes/adminRoutes.jsx
import { lazy } from "react";
import AdminLayout from "../layouts/AdminLayout";
import AdminRoute from "../components/auth/AdminRoute";

// Lazy-loaded admin pages
const AdminDashboard = lazy(() => import("../pages/admin/Dashboard"));
const ProblemList = lazy(() => import("../pages/admin/problems/ProblemList"));
const ProblemEditor = lazy(
  () => import("../pages/admin/problems/ProblemEditor"),
);
const TestCaseManager = lazy(
  () => import("../pages/admin/problems/TestCaseManager"),
);
const ContestList = lazy(() => import("../pages/admin/contests/ContestList"));
const ContestEditor = lazy(
  () => import("../pages/admin/contests/ContestEditor"),
);
const ContestProblems = lazy(
  () => import("../pages/admin/contests/ContestProblems"),
);
const ContestLive = lazy(() => import("../pages/admin/contests/ContestLive"));
const JudgeConfig = lazy(() => import("../pages/admin/execution/JudgeConfig"));
const LanguageConfig = lazy(
  () => import("../pages/admin/execution/LanguageConfig"),
);
const SubmissionList = lazy(
  () => import("../pages/admin/submissions/SubmissionList"),
);
const SubmissionDetail = lazy(
  () => import("../pages/admin/submissions/SubmissionDetail"),
);
const LeaderboardList = lazy(
  () => import("../pages/admin/leaderboards/LeaderboardList"),
);
const LeaderboardConfig = lazy(
  () => import("../pages/admin/leaderboards/LeaderboardConfig"),
);
const PlagiarismQueue = lazy(
  () => import("../pages/admin/plagiarism/PlagiarismQueue"),
);
const PlagiarismReview = lazy(
  () => import("../pages/admin/plagiarism/PlagiarismReview"),
);
const UserList = lazy(() => import("../pages/admin/users/UserList"));
const UserDetail = lazy(() => import("../pages/admin/users/UserDetail"));
const RoleManager = lazy(() => import("../pages/admin/users/RoleManager"));
const SystemDashboard = lazy(
  () => import("../pages/admin/system/SystemDashboard"),
);
const JudgeWorkers = lazy(() => import("../pages/admin/system/JudgeWorkers"));
const AIServices = lazy(() => import("../pages/admin/system/AIServices"));
const QueueMonitor = lazy(() => import("../pages/admin/system/QueueMonitor"));
const AuditLog = lazy(() => import("../pages/admin/system/AuditLog"));

export const adminRoutes = [
  {
    path: "/admin",
    element: (
      <AdminRoute>
        <AdminLayout />
      </AdminRoute>
    ),
    children: [
      // Dashboard
      { index: true, element: <AdminDashboard /> },

      // Problems
      { path: "problems", element: <ProblemList /> },
      { path: "problems/new", element: <ProblemEditor /> },
      { path: "problems/:id/edit", element: <ProblemEditor /> },
      { path: "problems/:id/testcases", element: <TestCaseManager /> },

      // Contests
      { path: "contests", element: <ContestList /> },
      { path: "contests/new", element: <ContestEditor /> },
      { path: "contests/:id/edit", element: <ContestEditor /> },
      { path: "contests/:id/problems", element: <ContestProblems /> },
      { path: "contests/:id/live", element: <ContestLive /> },

      // Execution
      { path: "execution/config", element: <JudgeConfig /> },
      { path: "execution/languages", element: <LanguageConfig /> },

      // Submissions
      { path: "submissions", element: <SubmissionList /> },
      { path: "submissions/:id", element: <SubmissionDetail /> },

      // Leaderboards
      { path: "leaderboards", element: <LeaderboardList /> },
      { path: "leaderboards/:id/config", element: <LeaderboardConfig /> },

      // Plagiarism
      { path: "plagiarism", element: <PlagiarismQueue /> },
      { path: "plagiarism/:id", element: <PlagiarismReview /> },

      // Users
      { path: "users", element: <UserList /> },
      { path: "users/:id", element: <UserDetail /> },
      { path: "roles", element: <RoleManager /> },

      // System
      { path: "system", element: <SystemDashboard /> },
      { path: "system/judges", element: <JudgeWorkers /> },
      { path: "system/ai", element: <AIServices /> },
      { path: "system/queues", element: <QueueMonitor /> },
      { path: "system/audit", element: <AuditLog /> },
    ],
  },
];
```

### 2. Component Breakdown

```
frontend/src/
├── components/
│   └── admin/
│       ├── layout/
│       │   ├── AdminSidebar.jsx        # Collapsible sidebar navigation
│       │   ├── AdminHeader.jsx         # Top bar with user menu
│       │   ├── AdminBreadcrumb.jsx     # Dynamic breadcrumb
│       │   └── AdminFooter.jsx         # Status bar
│       │
│       ├── common/
│       │   ├── DataTable.jsx           # Reusable sortable/filterable table
│       │   ├── FilterBar.jsx           # Universal filter component
│       │   ├── SearchInput.jsx         # Debounced search field
│       │   ├── Pagination.jsx          # Page navigation
│       │   ├── ConfirmModal.jsx        # Destructive action confirmation
│       │   ├── Drawer.jsx              # Slide-out panel
│       │   ├── StatusBadge.jsx         # Colored status indicators
│       │   ├── ActionMenu.jsx          # Dropdown action menu
│       │   ├── BulkActionBar.jsx       # Multi-select action bar
│       │   ├── EmptyState.jsx          # No data placeholder
│       │   ├── LoadingState.jsx        # Skeleton loaders
│       │   └── ErrorBoundary.jsx       # Error handling
│       │
│       ├── forms/
│       │   ├── FormField.jsx           # Labeled input wrapper
│       │   ├── MarkdownEditor.jsx      # Problem statement editor
│       │   ├── CodeEditor.jsx          # Test case input editor
│       │   ├── TagInput.jsx            # Multi-tag selector
│       │   ├── DateTimePicker.jsx      # Date/time input
│       │   ├── DurationInput.jsx       # Time duration picker
│       │   └── FileUpload.jsx          # File upload component
│       │
│       ├── problems/
│       │   ├── ProblemForm.jsx         # Create/edit form
│       │   ├── DifficultySelector.jsx  # Difficulty with AI suggestion
│       │   ├── ConstraintsEditor.jsx   # Time/memory limits
│       │   ├── TestCaseList.jsx        # Test case table
│       │   ├── TestCaseForm.jsx        # Individual test case
│       │   └── TestCaseGenerator.jsx   # Auto-generate test cases
│       │
│       ├── contests/
│       │   ├── ContestForm.jsx         # Contest settings
│       │   ├── ContestStatusBadge.jsx  # Lifecycle status
│       │   ├── ProblemMapper.jsx       # Drag-drop problem assignment
│       │   ├── ScheduleEditor.jsx      # Date/time configuration
│       │   ├── ScoringConfig.jsx       # ICPC/IOI settings
│       │   └── LiveMonitor.jsx         # Real-time contest view
│       │
│       ├── submissions/
│       │   ├── SubmissionTable.jsx     # Submission list
│       │   ├── SubmissionDetail.jsx    # Code + results view
│       │   ├── VerdictBadge.jsx        # AC/WA/TLE/etc badges
│       │   └── ExecutionLog.jsx        # Detailed execution info
│       │
│       ├── leaderboards/
│       │   ├── LeaderboardTable.jsx    # Ranking display
│       │   ├── FreezeControls.jsx      # Freeze/unfreeze buttons
│       │   └── ScoreOverride.jsx       # Manual adjustment form
│       │
│       ├── plagiarism/
│       │   ├── CaseQueue.jsx           # Review queue table
│       │   ├── ComparisonView.jsx      # Side-by-side diff
│       │   ├── SimilarityBadge.jsx     # Percentage indicator
│       │   └── DecisionForm.jsx        # Admin action form
│       │
│       ├── users/
│       │   ├── UserTable.jsx           # User list
│       │   ├── UserProfile.jsx         # User detail card
│       │   ├── ModerationPanel.jsx     # Ban/suspend controls
│       │   └── PermissionMatrix.jsx    # Role permission editor
│       │
│       └── system/
│           ├── ServiceCard.jsx         # Service health card
│           ├── WorkerTable.jsx         # Judge worker list
│           ├── QueueChart.jsx          # Queue depth graph
│           ├── AuditTable.jsx          # Audit log table
│           └── AlertList.jsx           # Recent alerts
│
├── layouts/
│   └── AdminLayout.jsx                 # Main admin shell
│
├── pages/
│   └── admin/
│       ├── Dashboard.jsx
│       ├── problems/
│       ├── contests/
│       ├── execution/
│       ├── submissions/
│       ├── leaderboards/
│       ├── plagiarism/
│       ├── users/
│       └── system/
│
├── hooks/
│   └── admin/
│       ├── useAdminAuth.js             # Admin auth checks
│       ├── useDataTable.js             # Table state management
│       ├── useBulkActions.js           # Multi-select logic
│       ├── useConfirmation.js          # Confirmation modal state
│       └── useAuditLog.js              # Audit logging helper
│
└── services/
    └── admin/
        ├── problemApi.js               # Problem CRUD
        ├── contestApi.js               # Contest management
        ├── submissionApi.js            # Submission queries
        ├── userApi.js                  # User management
        ├── plagiarismApi.js            # Plagiarism detection
        ├── systemApi.js                # System monitoring
        └── auditApi.js                 # Audit log API
```

### 3. API Contracts

```yaml
# Admin API Contracts

# ============ PROBLEMS ============
POST /api/admin/problems
  Request:
    title: string (required)
    slug: string (auto-generated if empty)
    statement: string (markdown)
    category: string[]
    difficulty: "Easy" | "Medium" | "Hard"
    constraints: string
    hints: string[]
    timeLimitMs: number
    memoryLimitMB: number
    languageMultipliers: { [language]: { time: number, memory: number } }
    status: "draft" | "review" | "published" | "hidden"
  Response:
    id: string
    ...all fields
    createdAt: datetime
    createdBy: string

GET /api/admin/problems
  Query:
    page: number
    limit: number
    sort: string (field:asc|desc)
    search: string
    difficulty: string
    category: string
    status: string
  Response:
    items: Problem[]
    total: number
    page: number
    totalPages: number

GET /api/admin/problems/:id
PUT /api/admin/problems/:id
DELETE /api/admin/problems/:id

POST /api/admin/problems/:id/difficulty/analyze
  Response:
    suggested: "Easy" | "Medium" | "Hard"
    confidence: number
    factors: { name: string, impact: string, value: string }[]

# ============ TEST CASES ============
GET /api/admin/problems/:id/testcases
POST /api/admin/problems/:id/testcases
  Request:
    input: string
    expectedOutput: string
    isSample: boolean
    score: number
    explanation: string (optional)

PUT /api/admin/problems/:id/testcases/:tcId
DELETE /api/admin/problems/:id/testcases/:tcId

POST /api/admin/problems/:id/testcases/bulk
  Request:
    testcases: TestCase[]

POST /api/admin/problems/:id/testcases/generate
  Request:
    count: number
    minN: number
    maxN: number
  Response:
    generated: TestCase[]

# ============ CONTESTS ============
POST /api/admin/contests
GET /api/admin/contests
GET /api/admin/contests/:id
PUT /api/admin/contests/:id
DELETE /api/admin/contests/:id

POST /api/admin/contests/:id/publish
POST /api/admin/contests/:id/cancel
POST /api/admin/contests/:id/extend
  Request:
    additionalMinutes: number

GET /api/admin/contests/:id/problems
POST /api/admin/contests/:id/problems
  Request:
    problemId: string
    order: number
    points: number
DELETE /api/admin/contests/:id/problems/:problemId

PUT /api/admin/contests/:id/problems/reorder
  Request:
    orderedIds: string[]

# ============ SUBMISSIONS ============
GET /api/admin/submissions
  Query:
    userId: string
    problemId: string
    contestId: string
    verdict: string
    language: string
    startDate: datetime
    endDate: datetime

GET /api/admin/submissions/:id
POST /api/admin/submissions/:id/rejudge

# ============ LEADERBOARDS ============
GET /api/admin/leaderboards/:contestId
POST /api/admin/leaderboards/:contestId/freeze
POST /api/admin/leaderboards/:contestId/unfreeze
POST /api/admin/leaderboards/:contestId/recalculate
PUT /api/admin/leaderboards/:contestId/entries/:userId
  Request:
    scoreAdjustment: number
    reason: string
    hide: boolean

# ============ PLAGIARISM ============
GET /api/admin/plagiarism
  Query:
    status: "pending" | "reviewed" | "flagged" | "cleared"
    contestId: string
    problemId: string

GET /api/admin/plagiarism/:id
PUT /api/admin/plagiarism/:id/decision
  Request:
    decision: "clear" | "flag_both" | "flag_user1" | "flag_user2" | "escalate"
    notes: string
    consequences: { userId: string, action: string }[]

POST /api/admin/plagiarism/run
  Request:
    contestId: string (optional)
    problemId: string (optional)

# ============ USERS ============
GET /api/admin/users
POST /api/admin/users
GET /api/admin/users/:id
PUT /api/admin/users/:id
DELETE /api/admin/users/:id

POST /api/admin/users/:id/warn
  Request:
    reason: string
    severity: "low" | "medium" | "high"

POST /api/admin/users/:id/suspend
  Request:
    duration: number (days)
    reason: string

POST /api/admin/users/:id/ban
  Request:
    reason: string
    permanent: boolean

POST /api/admin/users/:id/unban
POST /api/admin/users/:id/reset-password

# ============ ROLES & PERMISSIONS ============
GET /api/admin/roles
POST /api/admin/roles
PUT /api/admin/roles/:id
DELETE /api/admin/roles/:id

GET /api/admin/permissions
PUT /api/admin/roles/:id/permissions
  Request:
    permissions: string[]

# ============ EXECUTION CONFIG ============
GET /api/admin/config/judge
PUT /api/admin/config/judge

GET /api/admin/config/languages
PUT /api/admin/config/languages/:id

# ============ SYSTEM ============
GET /api/admin/system/health
GET /api/admin/system/workers
POST /api/admin/system/workers/:id/restart

GET /api/admin/system/queues
POST /api/admin/system/queues/:name/clear

GET /api/admin/system/audit
  Query:
    adminId: string
    action: string
    targetType: string
    startDate: datetime
    endDate: datetime
```

### 4. MongoDB Schema Outlines

```javascript
// ============ PROBLEM SCHEMA ============
const ProblemSchema = new mongoose.Schema({
  title: { type: String, required: true, unique: true, maxlength: 200 },
  slug: { type: String, required: true, unique: true, lowercase: true },
  statement: { type: String, required: true }, // Markdown
  constraints: { type: String, required: true },
  category: [{ type: String, enum: ['Arrays', 'Strings', 'Trees', 'Graphs', 'DP', 'Math', ...] }],
  tags: [String],
  difficulty: { type: String, enum: ['Easy', 'Medium', 'Hard'], required: true },

  // Execution limits
  baseLimits: {
    timeLimitMs: { type: Number, default: 2000, min: 100, max: 30000 },
    memoryLimitMB: { type: Number, default: 256, min: 16, max: 1024 }
  },
  languageMultipliers: [{
    language: String,
    timeMultiplier: { type: Number, default: 1 },
    memoryMultiplier: { type: Number, default: 1 },
    enabled: { type: Boolean, default: true }
  }],

  hints: [{ order: Number, content: String }],

  status: {
    type: String,
    enum: ['draft', 'review', 'published', 'hidden'],
    default: 'draft'
  },

  // Statistics (computed)
  stats: {
    totalSubmissions: { type: Number, default: 0 },
    acceptedSubmissions: { type: Number, default: 0 },
    acceptanceRate: { type: Number, default: 0 }
  },

  // AI difficulty analysis
  difficultyAnalysis: {
    suggested: String,
    confidence: Number,
    factors: [{ name: String, impact: String, value: String }],
    analyzedAt: Date
  },

  // Audit
  createdBy: { type: mongoose.Schema.Types.ObjectId, ref: 'User' },
  updatedBy: { type: mongoose.Schema.Types.ObjectId, ref: 'User' },
  publishedAt: Date,

}, { timestamps: true });

// ============ TEST CASE SCHEMA ============
const TestCaseSchema = new mongoose.Schema({
  problem: { type: mongoose.Schema.Types.ObjectId, ref: 'Problem', required: true },
  input: { type: String, required: true },
  expectedOutput: { type: String, required: true },
  isSample: { type: Boolean, default: false },
  score: { type: Number, default: 0 }, // For partial scoring
  explanation: String, // Optional explanation for sample cases
  order: Number,

  createdBy: { type: mongoose.Schema.Types.ObjectId, ref: 'User' },
}, { timestamps: true });

// ============ CONTEST SCHEMA ============
const ContestSchema = new mongoose.Schema({
  title: { type: String, required: true, maxlength: 200 },
  description: String,
  bannerUrl: String,

  // Schedule
  schedule: {
    startTime: { type: Date, required: true },
    endTime: { type: Date, required: true },
    registrationStart: Date,
    registrationEnd: Date
  },

  // Visibility
  visibility: {
    type: String,
    enum: ['public', 'private', 'invite-only'],
    default: 'public'
  },
  accessCode: String, // For private/invite-only
  invitedUsers: [{ type: mongoose.Schema.Types.ObjectId, ref: 'User' }],

  // Problems
  problems: [{
    problem: { type: mongoose.Schema.Types.ObjectId, ref: 'Problem' },
    order: Number, // A, B, C...
    points: Number,
    addedAt: Date
  }],

  // Scoring
  scoring: {
    type: { type: String, enum: ['icpc', 'ioi', 'custom'], default: 'icpc' },
    penaltyPerWrong: { type: Number, default: 20 }, // minutes
    partialScoring: { type: Boolean, default: false },
    tiebreaker: { type: String, enum: ['totalTime', 'lastAccepted'], default: 'totalTime' }
  },

  // Leaderboard
  leaderboard: {
    frozen: { type: Boolean, default: false },
    frozenAt: Date,
    autoFreezeMinutes: { type: Number, default: 60 }, // Before end
    visibility: { type: String, enum: ['public', 'participants', 'hidden'], default: 'public' }
  },

  // Status
  status: {
    type: String,
    enum: ['draft', 'scheduled', 'live', 'ended', 'archived', 'cancelled'],
    default: 'draft'
  },

  // Audit
  createdBy: { type: mongoose.Schema.Types.ObjectId, ref: 'User' },
  publishedBy: { type: mongoose.Schema.Types.ObjectId, ref: 'User' },
  publishedAt: Date,

}, { timestamps: true });

// ============ SUBMISSION SCHEMA ============
const SubmissionSchema = new mongoose.Schema({
  user: { type: mongoose.Schema.Types.ObjectId, ref: 'User', required: true },
  problem: { type: mongoose.Schema.Types.ObjectId, ref: 'Problem', required: true },
  contest: { type: mongoose.Schema.Types.ObjectId, ref: 'Contest' }, // Optional

  code: { type: String, required: true },
  language: { type: String, required: true },

  // Results
  verdict: {
    type: String,
    enum: ['pending', 'running', 'accepted', 'wrong_answer', 'time_limit',
           'memory_limit', 'runtime_error', 'compilation_error', 'internal_error'],
    default: 'pending'
  },
  score: { type: Number, default: 0 }, // For partial scoring

  // Execution details
  execution: {
    timeMs: Number,
    memoryKB: Number,
    testCasesPassed: Number,
    totalTestCases: Number,
    firstFailedCase: Number,
    compilerOutput: String,
    runtimeOutput: String
  },

  // Per test case results
  testResults: [{
    testCase: { type: mongoose.Schema.Types.ObjectId, ref: 'TestCase' },
    passed: Boolean,
    timeMs: Number,
    memoryKB: Number,
    output: String, // Truncated
    error: String
  }],

  // Plagiarism
  plagiarism: {
    checked: { type: Boolean, default: false },
    flagged: { type: Boolean, default: false },
    similarTo: [{ type: mongoose.Schema.Types.ObjectId, ref: 'Submission' }],
    similarity: Number
  },

  // IP tracking
  metadata: {
    ip: String,
    userAgent: String
  },

}, { timestamps: true });

// ============ PLAGIARISM CASE SCHEMA ============
const PlagiarismCaseSchema = new mongoose.Schema({
  submissions: [{
    submission: { type: mongoose.Schema.Types.ObjectId, ref: 'Submission' },
    user: { type: mongoose.Schema.Types.ObjectId, ref: 'User' }
  }],
  problem: { type: mongoose.Schema.Types.ObjectId, ref: 'Problem' },
  contest: { type: mongoose.Schema.Types.ObjectId, ref: 'Contest' },

  similarity: Number,
  detectionMethod: [String], // ['ast', 'string', 'structural']

  status: {
    type: String,
    enum: ['pending', 'reviewing', 'cleared', 'flagged', 'escalated'],
    default: 'pending'
  },

  // Decision
  decision: {
    decidedBy: { type: mongoose.Schema.Types.ObjectId, ref: 'User' },
    decidedAt: Date,
    outcome: String,
    notes: String,
    consequences: [{
      user: { type: mongoose.Schema.Types.ObjectId, ref: 'User' },
      action: String, // 'warning', 'zero_score', 'ban', etc.
      appliedAt: Date
    }]
  },

}, { timestamps: true });

// ============ LEADERBOARD ENTRY SCHEMA ============
const LeaderboardEntrySchema = new mongoose.Schema({
  contest: { type: mongoose.Schema.Types.ObjectId, ref: 'Contest', required: true },
  user: { type: mongoose.Schema.Types.ObjectId, ref: 'User', required: true },

  rank: Number,
  score: Number,
  penalty: Number, // Total time in seconds

  problemResults: [{
    problem: { type: mongoose.Schema.Types.ObjectId, ref: 'Problem' },
    solved: Boolean,
    attempts: Number,
    solvedAt: Date, // Time from contest start
    points: Number
  }],

  // Admin adjustments
  adjustments: [{
    amount: Number,
    reason: String,
    adjustedBy: { type: mongoose.Schema.Types.ObjectId, ref: 'User' },
    adjustedAt: Date
  }],

  hidden: { type: Boolean, default: false }, // Admin can hide from public

}, { timestamps: true });

LeaderboardEntrySchema.index({ contest: 1, rank: 1 });
LeaderboardEntrySchema.index({ contest: 1, user: 1 }, { unique: true });

// ============ AUDIT LOG SCHEMA ============
const AuditLogSchema = new mongoose.Schema({
  admin: { type: mongoose.Schema.Types.ObjectId, ref: 'User', required: true },
  action: { type: String, required: true }, // 'problem.create', 'user.ban', etc.

  target: {
    type: { type: String }, // 'Problem', 'User', 'Contest', etc.
    id: mongoose.Schema.Types.ObjectId
  },

  before: mongoose.Schema.Types.Mixed, // State before change
  after: mongoose.Schema.Types.Mixed, // State after change

  metadata: {
    ip: String,
    userAgent: String,
    reason: String // Optional reason for action
  },

}, { timestamps: true });

AuditLogSchema.index({ admin: 1, createdAt: -1 });
AuditLogSchema.index({ 'target.type': 1, 'target.id': 1 });
AuditLogSchema.index({ action: 1, createdAt: -1 });

// ============ JUDGE CONFIG SCHEMA ============
const JudgeConfigSchema = new mongoose.Schema({
  // Singleton - only one document
  _id: { type: String, default: 'global' },

  global: {
    maxConcurrent: { type: Number, default: 50 },
    queueTimeout: { type: Number, default: 300 },
    maxOutputSize: { type: Number, default: 64 }, // KB
    maxSourceSize: { type: Number, default: 128 } // KB
  },

  behavior: {
    onCompileError: { type: String, enum: ['stop', 'continue'], default: 'stop' },
    onRuntimeError: { type: String, enum: ['stop', 'continue'], default: 'stop' },
    onTLE: { type: String, enum: ['stop', 'continue'], default: 'continue' },
    comparisonMode: {
      type: String,
      enum: ['exact', 'ignore_whitespace', 'ignore_trailing', 'custom'],
      default: 'ignore_trailing'
    },
    floatTolerance: { type: Number, default: 1e-6 }
  },

  rateLimits: {
    perUserPerProblemPerMinute: { type: Number, default: 5 },
    perUserPerHour: { type: Number, default: 100 },
    globalPerMinute: { type: Number, default: 500 }
  },

  updatedBy: { type: mongoose.Schema.Types.ObjectId, ref: 'User' },

}, { timestamps: true });

// ============ LANGUAGE CONFIG SCHEMA ============
const LanguageConfigSchema = new mongoose.Schema({
  name: { type: String, required: true, unique: true },
  version: String,
  compiler: String,

  timeMultiplier: { type: Number, default: 1 },
  memoryMultiplier: { type: Number, default: 1 },

  enabled: { type: Boolean, default: true },

  // Piston/Judge0 specific
  runtime: String,
  compileCommand: String,
  runCommand: String,

}, { timestamps: true });

// ============ USER MODERATION SCHEMA (extend User) ============
// Add to existing User schema
const UserModerationSchema = {
  moderation: {
    status: {
      type: String,
      enum: ['active', 'warned', 'suspended', 'banned'],
      default: 'active'
    },
    suspendedUntil: Date,
    bannedAt: Date,
    bannedReason: String,

    warnings: [{
      reason: String,
      severity: String,
      issuedBy: { type: mongoose.Schema.Types.ObjectId, ref: 'User' },
      issuedAt: Date
    }],

    flags: [{
      type: String, // 'plagiarism', 'spam', 'abuse', etc.
      reason: String,
      flaggedBy: { type: mongoose.Schema.Types.ObjectId, ref: 'User' },
      flaggedAt: Date,
      resolved: Boolean,
      resolvedAt: Date
    }]
  }
};

// ============ ROLE & PERMISSION SCHEMA ============
const RoleSchema = new mongoose.Schema({
  name: { type: String, required: true, unique: true },
  description: String,

  permissions: [{
    resource: String, // 'problems', 'contests', 'users', etc.
    actions: [String] // ['create', 'read', 'update', 'delete']
  }],

  isSystem: { type: Boolean, default: false }, // Can't be deleted

}, { timestamps: true });

// Pre-defined permissions
const PERMISSIONS = {
  PROBLEMS: {
    CREATE: 'problems:create',
    READ: 'problems:read',
    UPDATE: 'problems:update',
    DELETE: 'problems:delete',
    PUBLISH: 'problems:publish'
  },
  CONTESTS: {
    CREATE: 'contests:create',
    READ: 'contests:read',
    UPDATE: 'contests:update',
    DELETE: 'contests:delete',
    PUBLISH: 'contests:publish',
    MANAGE_LIVE: 'contests:manage_live'
  },
  USERS: {
    READ: 'users:read',
    UPDATE: 'users:update',
    BAN: 'users:ban',
    MANAGE_ROLES: 'users:manage_roles'
  },
  SUBMISSIONS: {
    READ: 'submissions:read',
    REJUDGE: 'submissions:rejudge'
  },
  PLAGIARISM: {
    READ: 'plagiarism:read',
    REVIEW: 'plagiarism:review',
    DECIDE: 'plagiarism:decide'
  },
  LEADERBOARD: {
    READ: 'leaderboard:read',
    FREEZE: 'leaderboard:freeze',
    ADJUST: 'leaderboard:adjust'
  },
  SYSTEM: {
    READ: 'system:read',
    CONFIGURE: 'system:configure',
    AUDIT: 'system:audit'
  }
};
```

### 5. Role-Based Access Control (RBAC) Strategy

```javascript
// ============ RBAC Implementation ============

// 1. Pre-defined Roles
const SYSTEM_ROLES = {
  SUPER_ADMIN: {
    name: "Super Admin",
    permissions: ["*"], // All permissions
    isSystem: true,
  },
  ADMIN: {
    name: "Admin",
    permissions: [
      "problems:*",
      "contests:*",
      "users:read",
      "users:update",
      "submissions:*",
      "plagiarism:*",
      "leaderboard:*",
      "system:read",
    ],
    isSystem: true,
  },
  PROBLEM_SETTER: {
    name: "Problem Setter",
    permissions: [
      "problems:create",
      "problems:read",
      "problems:update",
      "submissions:read",
    ],
    isSystem: true,
  },
  MODERATOR: {
    name: "Moderator",
    permissions: [
      "users:read",
      "users:update",
      "submissions:read",
      "plagiarism:read",
      "plagiarism:review",
    ],
    isSystem: true,
  },
  CONTEST_MANAGER: {
    name: "Contest Manager",
    permissions: [
      "contests:*",
      "problems:read",
      "leaderboard:*",
      "submissions:read",
    ],
    isSystem: true,
  },
};

// 2. Middleware Implementation
// backend/src/middleware/adminAuth.js

import { User } from "../models/User.js";
import { Role } from "../models/Role.js";

// Verify user is admin (has any admin role)
export const requireAdmin = async (req, res, next) => {
  try {
    const user = await User.findById(req.user.id).populate("role");

    if (!user || user.role?.name === "user") {
      return res.status(403).json({
        message: "Admin access required",
      });
    }

    req.adminUser = user;
    req.permissions = await getPermissionsForRole(user.role);
    next();
  } catch (error) {
    res.status(500).json({ message: "Authorization error" });
  }
};

// Check specific permission
export const requirePermission = (...requiredPermissions) => {
  return (req, res, next) => {
    const userPermissions = req.permissions || [];

    // Super admin bypass
    if (userPermissions.includes("*")) {
      return next();
    }

    const hasPermission = requiredPermissions.every((perm) => {
      // Check exact match
      if (userPermissions.includes(perm)) return true;

      // Check wildcard (e.g., 'problems:*' matches 'problems:create')
      const [resource, action] = perm.split(":");
      return userPermissions.includes(`${resource}:*`);
    });

    if (!hasPermission) {
      return res.status(403).json({
        message: "Insufficient permissions",
        required: requiredPermissions,
      });
    }

    next();
  };
};

// 3. Route Protection Examples
// backend/src/routes/admin/problems.js

import { Router } from "express";
import { requireAdmin, requirePermission } from "../../middleware/adminAuth.js";

const router = Router();

// All admin routes require admin role
router.use(requireAdmin);

// List problems - requires read permission
router.get("/", requirePermission("problems:read"), problemController.list);

// Create problem - requires create permission
router.post(
  "/",
  requirePermission("problems:create"),
  problemController.create,
);

// Publish problem - requires publish permission
router.post(
  "/:id/publish",
  requirePermission("problems:publish"),
  problemController.publish,
);

// Delete problem - requires delete permission + confirmation
router.delete(
  "/:id",
  requirePermission("problems:delete"),
  problemController.delete,
);

// 4. Frontend Permission Check Hook
// frontend/src/hooks/admin/usePermission.js

import { useAuth } from "../../context/AuthContext";

export const usePermission = () => {
  const { user } = useAuth();

  const hasPermission = (permission) => {
    if (!user?.permissions) return false;
    if (user.permissions.includes("*")) return true;

    const [resource, action] = permission.split(":");
    return (
      user.permissions.includes(permission) ||
      user.permissions.includes(`${resource}:*`)
    );
  };

  const hasAnyPermission = (...permissions) => {
    return permissions.some((p) => hasPermission(p));
  };

  const hasAllPermissions = (...permissions) => {
    return permissions.every((p) => hasPermission(p));
  };

  return { hasPermission, hasAnyPermission, hasAllPermissions };
};

// 5. Frontend Protected Component
// frontend/src/components/admin/common/PermissionGate.jsx

export const PermissionGate = ({ permission, fallback = null, children }) => {
  const { hasPermission } = usePermission();

  if (!hasPermission(permission)) {
    return fallback;
  }

  return children;
};

// Usage example:
// <PermissionGate permission="problems:delete">
//   <DeleteButton />
// </PermissionGate>

// 6. Audit Logging Middleware
// backend/src/middleware/auditLog.js

export const auditLog = (action, getTarget) => {
  return async (req, res, next) => {
    const originalSend = res.send;
    const startState = getTarget ? await getTarget(req) : null;

    res.send = function (body) {
      // Log after successful response
      if (res.statusCode >= 200 && res.statusCode < 300) {
        AuditLog.create({
          admin: req.adminUser._id,
          action,
          target: {
            type: req.baseUrl.split("/").pop(),
            id: req.params.id,
          },
          before: startState,
          after: JSON.parse(body),
          metadata: {
            ip: req.ip,
            userAgent: req.headers["user-agent"],
          },
        }).catch(console.error);
      }

      return originalSend.call(this, body);
    };

    next();
  };
};

// Usage:
router.put(
  "/:id",
  requirePermission("problems:update"),
  auditLog("problem.update", async (req) => Problem.findById(req.params.id)),
  problemController.update,
);
```

---

## D. Use Case Mapping Summary

| Admin Use Case                 | Module           | Primary Route                        | Key Actions                     |
| ------------------------------ | ---------------- | ------------------------------------ | ------------------------------- |
| Maintain contests and problems | Contest, Problem | `/admin/contests`, `/admin/problems` | CRUD, publish, archive          |
| Decide problem difficulty      | Problem          | `/admin/problems/:id/edit`           | Manual + AI-assisted            |
| Set memory/time limits         | Problem          | `/admin/problems/:id/edit`           | Language-aware config           |
| Manage test cases              | Problem          | `/admin/problems/:id/testcases`      | Sample vs hidden, bulk import   |
| Add problems                   | Problem          | `/admin/problems/new`                | Full editor with markdown       |
| Maintain platform services     | System           | `/admin/system/*`                    | Health check, restart workers   |
| View/manage leaderboards       | Leaderboard      | `/admin/leaderboards/*`              | Freeze, recalculate, adjust     |
| Plagiarism detection           | Plagiarism       | `/admin/plagiarism`                  | Review queue, side-by-side diff |

---

## E. Implementation Priority

### Phase 1 (MVP)

1. Admin authentication & RBAC
2. Problem CRUD with basic test cases
3. User management (view, ban)
4. Audit logging

### Phase 2

1. Contest management
2. Advanced test case management
3. Leaderboard controls
4. Submission browser

### Phase 3

1. Plagiarism detection integration
2. System monitoring dashboard
3. AI difficulty analysis
4. Bulk operations

### Phase 4

1. Advanced analytics
2. Queue management
3. Custom role builder
4. API rate limiting dashboard
